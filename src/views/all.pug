html 
    head 
        link(rel='stylesheet' href='/styles/mainStyle.css')
        title All Users
    body
        script(src="/styles/themes.js")
        include ./header.pug
        if(type == "error")
            h3(style = "color:red") #{message} 
        else 
            h2(style = "color:blue") Check out all the Squeakers in the Horde: 
            each user in output
                p 
                    | Username: #{user}

        br 
        br
        br
        a(href="/message") 
            button Go Back
        include ./footer.pug
