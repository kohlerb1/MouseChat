html 
    head 
        link(rel='stylesheet' href='/styles/mainStyle.css')
        include ./header.pug
        title Update Favorite Cheese 
    body
        script(src="/styles/themes.js")
        if(message)
            h4 #{message}
        h3 Enter your newly realized favorite cheese
        form(action = "/updateUserCheese" method = "POST")
            br
            input(name = "cheese" type = "text" required placeholder = "Favorite Cheese")
            br
            button(name =  type = "Submit") Update Cheese 
        br
        br
        form(action = "/settings" method = "GET")
            button(name =  type = "Submit") Settings 
        input(type="hidden" id="name" value=`${id}`)
        br
        include ./footer.pug
    script(src="https://cdn.socket.io/4.5.4/socket.io.min.js")
    script(src="/public/clientDefault.js")
