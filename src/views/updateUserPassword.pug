html 
    head 
        link(rel='stylesheet' href='/styles/mainStyle.css')
        include ./header.pug
        title Update Password 
    body
        if(message)
            h4 #{message}
        h3 Enter your new username
        form(action = "/updateUserPassword" method = "POST")
            br
            input(name = "password" type = "password" required placeholder = "new password")
            br
            button(name =  type = "Submit") Update Password 
        br
        br
        br
        include ./footer.pug


